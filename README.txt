1. clone the project or download zip file from github
2. import the project in eclipse
3. run the code in two ways
a. first you can simply run the testng.xml 
b. open cmd : relocate to the path of the project: if you maven maven install in your machine
just type: mvn test